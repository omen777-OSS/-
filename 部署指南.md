# 智诊通 · 云端部署指南

> 部署后，任何人用手机浏览器打开网址即可使用，无需安装任何 App。

---

## 方案一：Render 部署（推荐，免费，5 分钟搞定）

Render 是一个免费的云托管平台，支持 Python 服务，免费额度足够日常使用。

### 准备工作

1. 一个 **GitHub 账号**（免费注册：https://github.com）
2. 一个 **DeepSeek API 密钥**（在 https://platform.deepseek.com 申请）

### 步骤 1：上传代码到 GitHub

1. 登录 GitHub，点击右上角 `+` → `New repository`
2. 仓库名填 `zhizhentong`，选 `Public`，点击 `Create repository`
3. 把以下文件上传到仓库（点击 `uploading an existing file`，拖拽上传）：
   - `zhizhentong.html`
   - `start_server.py`
   - `requirements.txt`
   - `Procfile`
   - `render.yaml`
4. 点击 `Commit changes` 完成上传

### 步骤 2：在 Render 上创建服务

1. 访问 https://render.com，用 GitHub 账号登录
2. 点击右上角 `New +` → `Web Service`
3. 选择你刚创建的 `zhizhentong` 仓库，点击 `Connect`
4. 配置页面填写：
   - **Name**：`zhizhentong-ai`（这个会成为网址的一部分）
   - **Runtime**：`Python 3`
   - **Build Command**：`pip install -r requirements.txt`
   - **Start Command**：`python start_server.py`
   - **Instance Type**：选 `Free`（免费）
5. 点击 **Advanced** → 添加环境变量：
   - Key: `LLM_API_KEY`，Value: 粘贴你的 DeepSeek API 密钥
   - Key: `LLM_BASE_URL`，Value: `https://api.deepseek.com/v1`
   - Key: `LLM_MODEL`，Value: `deepseek-chat`
6. 点击 `Create Web Service`，等待 2-3 分钟部署完成

### 步骤 3：获取网址并分享

部署完成后，Render 会显示一个网址，形如：
```
https://zhizhentong-ai.onrender.com
```

把这个网址发给任何人，他们用手机浏览器打开就能直接使用！

> **注意**：Render 免费实例在 15 分钟无访问后会休眠，首次打开可能需要 10-30 秒唤醒，之后就正常了。

---

## 方案二：Railway 部署（更稳定，有免费额度）

Railway 也是一个云托管平台，新用户有 $5 免费额度，足够用几个月。

1. 访问 https://railway.app，用 GitHub 登录
2. 点击 `New Project` → `Deploy from GitHub repo`
3. 选择你的 `zhizhentong` 仓库
4. 部署后，在 `Variables` 中添加：
   - `LLM_API_KEY` = 你的 DeepSeek 密钥
   - `LLM_BASE_URL` = `https://api.deepseek.com/v1`
   - `LLM_MODEL` = `deepseek-chat`
5. 在 `Settings` → `Networking` 中点击 `Generate Domain` 生成公网网址

---

## 方案三：本地局域网使用（不上云，只在同一 WiFi 内用）

如果不需要公网访问，只是在办公室/家里同一 WiFi 内用：

1. 双击 `启动智诊通.bat`
2. 首次运行输入 DeepSeek API 密钥
3. 窗口中会显示局域网地址，形如 `http://192.168.1.100:8765`
4. 手机连同一 WiFi，浏览器打开这个地址即可

---

## 常见问题

### Q：部署后大模型不能用？
A：检查环境变量 `LLM_API_KEY` 是否正确填写，密钥是否有余额。可以在页面设置中点"测试连接"查看具体错误。

### Q：可以换其他大模型吗？
A：可以。修改环境变量：
- `LLM_BASE_URL`：接口地址（OpenAI 兼容格式）
- `LLM_MODEL`：模型名称

支持所有 OpenAI 兼容接口，包括 OpenAI、通义千问、智谱、Moonshot 等。

### Q：API 密钥安全吗？
A：密钥只存在于服务端环境变量中，前端页面永远看不到密钥。所有大模型请求都经过服务端代理转发。

### Q：免费额度够用吗？
A：Render 免费实例每月 750 小时运行时间（等于一直开着），100GB 带宽。DeepSeek API 按 token 计费，非常便宜，一般使用每月几块钱。

### Q：怎么更新代码？
A：修改本地文件后，重新上传到 GitHub，Render 会自动重新部署（Auto Deploy 已开启）。

---

## 文件说明

| 文件 | 用途 |
|------|------|
| `zhizhentong.html` | 前端页面（问诊界面） |
| `start_server.py` | 后端服务（API 代理，同时支持本地和云端） |
| `requirements.txt` | Python 依赖（仅标准库，无额外依赖） |
| `Procfile` | 云平台启动命令 |
| `render.yaml` | Render 一键部署配置 |
| `启动智诊通.bat` | Windows 本地双击启动 |
| `部署指南.md` | 本文档 |
